#the circle module has functions that perform
#calculations related to circles.
import math

#the area function accepts a circle's reduis as an
#argument and return the area of the circle.
def area(radius):
    return math.pi * radius**2

    #The circumference function accepts a circle's radius
    #ans returns the area of the circle.
def area(radius):
    return math.pi * radius






#the rectangle module has function that perform
#calculations related o rectangles.

#the srae function accept a rectangle's width and
#lenght as argument and returns the rectangle's area
def area(width,lenght):
    return width * lenght

#the perimeter function accepts a rectangle's width
#and length as arguments and returns the rectangle's perimeter.

def perimeter(width, length):
    return 2 * (width + length)